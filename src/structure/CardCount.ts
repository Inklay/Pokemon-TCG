/**
 * @interface CardCount
 * 
 * @property {number} quantity - The number of time the user has this card
 * @property {number} cardNumber - The crad number in it's serie 
 */
export interface CardCount {
  quantity: number
  cardNumber: number
}
